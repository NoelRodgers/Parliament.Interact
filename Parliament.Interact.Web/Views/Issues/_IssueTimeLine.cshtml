@model Parliament.Interact.Web.Models.IssueViewModel

<div class="row">
    @if (Model.TimeLines != null) {
        foreach (var timeLineItem in Model.TimeLines)
        {
            <div class="col-md-3">
                @Html.Raw(timeLineItem.HTMLContent)
            </div>
        }
    }

</div>